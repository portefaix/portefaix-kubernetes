# SPDX-FileCopyrightText: Copyright (C) Nicolas Lamirault <nicolas.lamirault@gmail.com>
# SPDX-License-Identifier: Apache-2.0

---
# yaml-language-server: $schema=https://json.schemastore.org/github-workflow.json
name: Project / Issues and PR labels required

on: # yamllint disable-line rule:truthy
  issues:
    types: [opened]
  pull_request_target:
    types: [opened]
    # schedule:
    #   - cron: "0 0 * * *"

jobs:
  required_labels:
    runs-on: ubuntu-latest
    steps:
    - name: Monitor Actions
      uses: GitHubSecurityLab/actions-permissions/monitor@bf82d13b9b10051d224345ab9184f5ede0a94289 # v1.0.2-beta9
      with:
        config: ${{ vars.PERMISSIONS_CONFIG }}

    - name: Require prefix kind
      uses: trstringer/require-label-prefix@1be29e66641a665b1243f6356f9fcdb374e99aaf # v1.0.1
      with:
        secret: ${{ github.TOKEN }}
        prefix: kind
        addLabel: true
        defaultLabel: needs/kind

    - name: Require prefix area
      uses: trstringer/require-label-prefix@1be29e66641a665b1243f6356f9fcdb374e99aaf # v1.0.1
      with:
        secret: ${{ github.TOKEN }}
        prefix: area
        addLabel: true
        defaultLabel: needs/area

    - name: Require prefix priority
      uses: trstringer/require-label-prefix@1be29e66641a665b1243f6356f9fcdb374e99aaf # v1.0.1
      with:
        secret: ${{ github.TOKEN }}
        prefix: priority
        addLabel: true
        defaultLabel: needs/priority

    # - name: Require prefix size
    #   uses: trstringer/require-label-prefix@v1
    #   with:
    #     secret: ${{ github.TOKEN }}
    #     prefix: size
    #     addLabel: true
    #     defaultLabel: needs/size

    - name: Require prefix status
      uses: trstringer/require-label-prefix@1be29e66641a665b1243f6356f9fcdb374e99aaf # v1.0.1
      with:
        secret: ${{ github.TOKEN }}
        prefix: status
        addLabel: true
        defaultLabel: needs/status

---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: kube-prometheus-stack
  namespace: flux-system
spec:
  # chart:
  #   spec:
  #     version: 12.0.4
  values:
    alertmanager:
      ingress:
        enabled: true
        annotations:
          kubernetes.io/ingress.class: nginx
          # cert-manager.io/cluster-issuer: 'letsencrypt-prod'
          nginx.ingress.kubernetes.io/auth-url: 'https://portefaix.xyz/oauth2/auth'
          nginx.ingress.kubernetes.io/auth-signin: 'https://portefaix.xyz/oauth2/start?rd=$escaped_request_uri'
        hosts:
          - alertmanager.portefaix.xyz
        path: /
        # tls:
        #   - secretName: alertmanager.portefaix.xyz-tls
        #     hosts:
        #       - alertmanager.portefaix.xyz

    grafana:
      ingress:
        enabled: true
        annotations:
          kubernetes.io/ingress.class: nginx
          # cert-manager.io/cluster-issuer: 'letsencrypt-prod'
          nginx.ingress.kubernetes.io/auth-url: 'https://portefaix.xyz/oauth2/auth'
          nginx.ingress.kubernetes.io/auth-signin: 'https://portefaix.xyz/oauth2/start?rd=$escaped_request_uri'
        hosts:
          - grafana.portefaix.xyz
        path: /
        # tls:
        #   - secretName: grafana.portefaix.xyz-tls
        #     hosts:
        #       - grafana.portefaix.xyz

    prometheus:
      ingress:
        enabled: true
        annotations:
          kubernetes.io/ingress.class: nginx
          # cert-manager.io/cluster-issuer: 'letsencrypt-prod'
          nginx.ingress.kubernetes.io/auth-url: 'https://portefaix.xyz/oauth2/auth'
          nginx.ingress.kubernetes.io/auth-signin: 'https://portefaix.xyz/oauth2/start?rd=$escaped_request_uri'
        hosts:
          - prometheus.portefaix.xyz
        path: /
        # tls:
        #   - secretName: prometheus.portefaix.xyz-tls
        #     hosts:
        #       - prometheus.portefaix.xyz
      prometheusSpec:
        storageSpec:
          volumeClaimTemplate:
            spec:
              storageClassName: "standard"
              resources:
                requests:
                  storage: 1Gi

---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: external-dns
  namespace: flux-system
spec:
  interval: 5m
  chart:
    spec:
      chart: external-dns
      sourceRef:
        kind: HelmRepository
        name: bitnami
        namespace: flux-system
      version: 4.0.0
  releaseName: external-dns
  targetNamespace: dns
  interval: 5m
  values:
    metrics:
      enabled: true
      serviceMonitor:
        enabled: true

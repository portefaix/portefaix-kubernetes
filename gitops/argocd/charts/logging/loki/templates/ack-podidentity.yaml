# SPDX-FileCopyrightText: Copyright (C) Nicolas Lamirault <nicolas.lamirault@gmail.com>
# SPDX-License-Identifier: Apache-2.0

{{ if .Values.ack.enabled -}}
---
apiVersion: eks.services.k8s.aws/v1alpha1
kind: PodIdentityAssociation
metadata:
  name: {{ .Values.ack.clusterName }}-loki-pod-identity
  namespace: {{ .Release.Namespace }}
spec:
  clusterName: {{ .Values.ack.clusterName }}
  namespace: {{ .Release.Namespace }}
  serviceAccount: {{ .Values.ack.serviceAccount.name }}
  roleARN: {{ .Values.ack.serviceAccount.roleArn }}
  roleRef:
    name: {{ .Values.ack.clusterName }}-loki-role
  tags:
    {{- toYaml .Values.ack.tags | nindent 4 }}
{{- end }}

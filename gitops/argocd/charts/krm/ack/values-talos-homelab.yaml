---
ack-chart:
  apigatewayv2:
    deployment:
      replicas: 1
    # TODO:
    # metrics:
    #   serviceMonitor:
    #     relabelings:
    #     - action: replace
    #       replacement: portefaix-talos-homelab
    #       targetLabel: cluster
    resources:
      requests:
        memory: "64Mi"
        cpu: "50m"
      limits:
        memory: "128Mi"
        cpu: "100m"
